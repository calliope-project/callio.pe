[workspace]
authors = ["Stefan Pfenninger <stefan@pfenninger.org>"]
channels = ["conda-forge"]
name = "callio.pe"
platforms = ["osx-arm64", "osx-64", "linux-64", "win-64"]
version = "0.1.0"

[tasks]
dev = { cmd = "hugo server -D"}
public = { cmd = "hugo --minify"}
clean = { cmd = "rm -rf public" }

[dependencies]
hugo = "0.143.1.*"
